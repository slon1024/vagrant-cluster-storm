- hosts: all
  remote_user: vagrant
  user: root
  sudo: true
  roles:
    - common

- hosts: zookeeper
  remote_user: vagrant
  user: root
  sudo: true
  roles:
    - zookeeper

- hosts: storm
  remote_user: vagrant
  user: root
  sudo: true
  roles:
    - storm

- hosts: storm_master
  remote_user: vagrant
  user: root
  sudo: true
  roles:
    - storm_master

- hosts: kafka
  remote_user: vagrant
  user: root
  sudo: true
  roles:
    - kafka