#!/bin/bash -l

HOME=/home/{{ redis['user'] }} {{ nodejs['home_dir'] }}/bin/node {{ nodejs['home_dir'] }}/bin/redis-commander --redis-port {{ redis['port'] }} --redis-host {% for host in groups['redis'] %}{{ hostvars[host].local_ipv4 }}{% endfor %} --port {{ redis_commander['port'] }}